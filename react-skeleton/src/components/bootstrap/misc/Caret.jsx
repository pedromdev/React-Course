var React = require('react');

var Caret = React.createClass({
    render: function() {
        return (
            <span className="caret"></span>
        );
    }
});

module.exports = Caret;
